{
  "name": "ai-webpage-summarizer",
  "version": "1.0.0",
  "description": "Goal is to create an application that is a web-based summarizer using Gemma3 as the LLM model, BeautifulSoup as the web scraper, and the front end being react",
  "main": "web_summarizer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "debug": "react-scripts start --inspect"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "author": "",
  "license": "ISC",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
